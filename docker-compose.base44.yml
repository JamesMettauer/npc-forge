services:
  app:
    image: node:22-bookworm-slim
    working_dir: /app
    command: sh -lc "npm install && npx --yes base44@latest dev"
    ports:
      - "3000:3000"
    env_file:
      - /run/base44/app.env
    environment:
      CHOKIDAR_USEPOLLING: "true"
      npm_config_min_release_age: "7"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - npm_cache:/root/.npm
    stdin_open: true
    tty: true
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:3000/').then(r => { if (!r.ok) process.exit(1) }).catch(() => process.exit(1))"
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 30s

volumes:
  node_modules:
  npm_cache:
