import { Swords, Theater, Sparkles, Zap, MessagesSquare, ScrollText } from 'lucide-react';
const choices=[['roleplay','Quick NPC','Create only what you need for a recurring character.',Theater],['combat','Detailed NPC','Build a complete NPC with statistics and roleplay depth.',Swords],['template','Start from a Template','Initialize a new character from a reusable foundation.',ScrollText],['auto','Generate from a Prompt','Describe the character and let NPC Forge prepare the first draft.',Sparkles],['quick','Quick Encounter','Create a ready-to-use ally, enemy, creature, or supporting NPC in seconds.',Zap],['interview','Guided Character Interview','Build the character with the Guild Scribe one question at a time.',MessagesSquare]];
export default function ModeChooser({onChoose}){return <div className="grid gap-4 sm:grid-cols-2">{choices.map(([id,title,desc,Icon])=><button key={id} onClick={()=>onChoose(id)} className="group rounded-2xl border border-border bg-card p-6 text-left transition hover:-translate-y-0.5 hover:border-brand/40"><Icon className="mb-5 text-brand"/><h3 className="font-fantasy text-xl">{title}</h3><p className="mt-1 text-sm text-muted-foreground">{desc}</p></button>)}</div>}
