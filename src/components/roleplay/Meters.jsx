const metrics=[['Relationship','relationship_score',-100,100],['Trust','trust',0,100],['Fear','fear',0,100],['Respect','respect',0,100],['Hostility','hostility',0,100]];
export default function Meters({conversation}){return <div className="space-y-3">{metrics.map(([label,key,min,max])=>{const v=conversation[key]||0,p=((v-min)/(max-min))*100;return <div key={key}><div className="mb-1 flex justify-between text-xs text-muted-foreground"><span>{label}</span><span>{v}</span></div><div className="h-1.5 rounded-full bg-muted"><div className="h-full rounded-full bg-brand transition-all" style={{width:`${Math.max(0,Math.min(100,p))}%`}}/></div></div>})}</div>}