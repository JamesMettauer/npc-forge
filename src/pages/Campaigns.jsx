import { useEffect, useState } from 'react';
import { base44 } from '@/api/base44Client';import PageHeader from '@/components/PageHeader';
import NavControls from '@/components/NavControls';
import { Link } from 'react-router-dom';
import { Users } from 'lucide-react';
export default function Campaigns(){const [items,setItems]=useState([]),[name,setName]=useState(''),[setting,setSetting]=useState('');useEffect(()=>{base44.entities.Campaign.list('-updated_date').then(setItems)},[]);const add=async()=>{const x=await base44.entities.Campaign.create({name,setting,status:'active'});setItems(p=>[x,...p]);setName('');setSetting('')};return <div className="p-5 sm:p-8"><NavControls/><PageHeader eyebrow="Worlds & stories" title="Campaigns"/><div className="mb-6 flex flex-col gap-3 rounded-2xl border border-border bg-card p-4 sm:flex-row"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Campaign name" className="field"/><input value={setting} onChange={e=>setSetting(e.target.value)} placeholder="Setting" className="field"/><button disabled={!name} onClick={add} className="rounded-lg bg-brand px-5 py-2 text-sm font-semibold text-brand-foreground disabled:opacity-40">Add campaign</button></div><div className="mb-6"><Link to="/player-characters?import=1" className="inline-flex items-center gap-1.5 rounded-lg border border-border px-4 py-2 text-sm font-medium text-foreground hover:bg-muted"><Users size={16}/>Add Player Character</Link></div><div className="grid gap-4 md:grid-cols-2">{items.map(c=><article key={c.id} className="rounded-2xl border border-border bg-card p-5"><div className="flex justify-between"><h2 className="font-serif text-xl">{c.name}</h2><span className="text-xs text-brand">{c.status}</span></div><p className="mt-2 text-sm text-muted-foreground">{c.setting||'No setting added'}</p></article>)}</div></div>}